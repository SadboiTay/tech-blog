<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            <span>{{user.username}}</span> on {{created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>